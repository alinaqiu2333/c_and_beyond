#include <stdio.h>
int main(){
	// gcc -Wall -o output_file compile_cmd_line.c 
	// the above command saves the output into a file called output_file
	// and it is excutable, this can be tested by using 
	// ls -F
	// the above command shows if a file is excutable
	printf("hello!\n");
}